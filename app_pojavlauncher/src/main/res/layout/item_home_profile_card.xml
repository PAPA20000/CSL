<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/card_profile_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="4dp"
    android:background="@drawable/bg_profile_card"
    android:clickable="true"
    android:focusable="true">

    <!-- Banner Background Image -->
    <ImageView
        android:id="@+id/img_profile_background"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <!-- Dark scrim over banner -->
    <View
        android:id="@+id/img_profile_background_scrim"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="#60000000"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Banner area with menu button and profile icon -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="80dp">

            <!-- Three-dot menu (top-right of banner) -->
            <ImageButton
                android:id="@+id/btn_profile_menu"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="top|end"
                android:layout_marginTop="4dp"
                android:layout_marginEnd="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_more_vert"
                android:contentDescription="@string/edit_profile"
                android:tint="#FFFFFF" />

            <!-- Profile Icon centered in banner -->
            <ImageView
                android:id="@+id/img_profile_icon"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_gravity="center"
                android:scaleType="fitCenter"
                android:background="@drawable/bg_profile_icon_rounded" />
        </FrameLayout>

        <!-- Text area -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="10dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:gravity="center_horizontal">

            <TextView
                android:id="@+id/tv_profile_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:ellipsize="end"
                android:maxLines="1"
                android:gravity="center" />

            <TextView
                android:id="@+id/tv_profile_version"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#CCCCCC"
                android:textSize="10sp"
                android:ellipsize="end"
                android:maxLines="1"
                android:layout_marginTop="1dp"
                android:gravity="center" />

            <TextView
                android:id="@+id/tv_profile_mod_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/accent_primary"
                android:textSize="9sp"
                android:ellipsize="end"
                android:maxLines="1"
                android:layout_marginTop="1dp"
                android:gravity="center" />

            <TextView
                android:id="@+id/tv_profile_ram"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#AAAAAA"
                android:textSize="9sp"
                android:ellipsize="end"
                android:maxLines="1"
                android:layout_marginTop="1dp"
                android:gravity="center" />
        </LinearLayout>

        <!-- Action buttons row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingHorizontal="6dp"
            android:paddingBottom="8dp">

            <!-- Browse Resources Button -->
            <FrameLayout
                android:id="@+id/btn_profile_browse"
                android:layout_width="0dp"
                android:layout_height="34dp"
                android:layout_weight="1"
                android:layout_marginEnd="4dp"
                android:background="@drawable/bg_browse_button"
                android:clickable="true"
                android:focusable="true">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_browse_resources"
                    android:tint="#39FF14" />
            </FrameLayout>

            <!-- Play Button -->
            <FrameLayout
                android:id="@+id/btn_profile_play"
                android:layout_width="0dp"
                android:layout_height="34dp"
                android:layout_weight="1"
                android:layout_marginStart="4dp"
                android:background="@drawable/bg_play_button_green"
                android:clickable="true"
                android:focusable="true">

                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_play_arrow"
                    android:tint="#121212" />
            </FrameLayout>
        </LinearLayout>

    </LinearLayout>

</FrameLayout>
