<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:padding="6dp">

    <!-- ═══════════════════════════════════════ -->
    <!-- ✦ SLIM PREMIUM DOWNLOAD PILL           -->
    <!-- ═══════════════════════════════════════ -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/download_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_download_card"
        android:paddingHorizontal="14dp"
        android:paddingVertical="10dp"
        app:layout_constraintWidth_max="480dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Slim kinetic ring + icon -->
        <FrameLayout
            android:id="@+id/progress_indicator_container"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <com.kdt.mcgui.KineticProgressView
                android:id="@+id/kinetic_progress"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <ImageView
                android:id="@+id/progress_icon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center"
                android:scaleType="centerInside"
                android:src="@drawable/ic_download"
                android:tint="#FFFFFF"
                tools:ignore="UseAppTint" />
        </FrameLayout>

        <!-- Status / Title -->
        <TextView
            android:id="@+id/progress_status_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Downloading Minecraft"
            android:textColor="#F0F0F3"
            android:textSize="12.5sp"
            android:fontFamily="sans-serif-medium"
            android:ellipsize="end"
            android:maxLines="1"
            android:letterSpacing="0.01"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            app:layout_constraintStart_toEndOf="@id/progress_indicator_container"
            app:layout_constraintEnd_toStartOf="@+id/progress_percentage_text"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Downloading Minecraft"/>

        <!-- Percentage — refined tabular display -->
        <TextView
            android:id="@+id/progress_percentage_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0%"
            android:textColor="#E4E4EA"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="-0.01"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="26%"/>

        <!-- Ultra-slim rounded progress bar -->
        <ProgressBar
            android:id="@+id/progress_horizontal_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="3.5dp"
            android:layout_marginTop="7dp"
            android:layout_marginStart="12dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/bg_download_progress_bar"
            app:layout_constraintStart_toEndOf="@id/progress_indicator_container"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/progress_status_text"/>

        <!-- Details Row (Metrics, Speed, ETA) — single muted line -->
        <TextView
            android:id="@+id/progress_detail_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="#8B8B96"
            android:textSize="10sp"
            android:layout_marginTop="5dp"
            android:layout_marginStart="12dp"
            android:ellipsize="end"
            android:maxLines="1"
            app:layout_constraintStart_toEndOf="@id/progress_indicator_container"
            app:layout_constraintEnd_toStartOf="@+id/progress_speed_text"
            app:layout_constraintTop_toBottomOf="@id/progress_horizontal_bar"
            tools:text="141 MB / 513 MB"/>

        <TextView
            android:id="@+id/progress_speed_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="#8B8B96"
            android:textSize="10sp"
            android:layout_marginTop="5dp"
            android:layout_marginHorizontal="4dp"
            app:layout_constraintStart_toEndOf="@id/progress_detail_text"
            app:layout_constraintEnd_toStartOf="@+id/progress_eta_text"
            app:layout_constraintTop_toBottomOf="@id/progress_horizontal_bar"
            tools:text="2.5 MB/s"/>

        <TextView
            android:id="@+id/progress_eta_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="#8B8B96"
            android:textSize="10sp"
            android:layout_marginTop="5dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/progress_horizontal_bar"
            tools:text="1m 20s"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- ═══════════════════════════════════════ -->
    <!-- ✦ LEGACY COMPATIBILITY VIEWS (HIDDEN)  -->
    <!-- ═══════════════════════════════════════ -->
    <com.kdt.mcgui.KineticProgressView
        android:id="@+id/kinetic_progress_legacy"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/progress_textview"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/progress_flip_arrow"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <LinearLayout
        android:id="@+id/progress_linear_layout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:orientation="vertical">
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
